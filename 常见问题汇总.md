# 常见问题汇总

本文档整理项目运行、训练和预测中常见的问题。内容以当前仓库为准。

## 1. 环境配置

### TensorFlow 和 protobuf 报错

**问题：运行 `predict.py` 时出现：**

```text
TypeError: Descriptors cannot not be created directly.
```

**处理：**  
这是 TensorFlow 2.2.0 与高版本 `protobuf` 不兼容导致的。本项目已在 `requirements.txt` 中锁定：

```text
protobuf==3.20.3
```

可以执行：

```bash
pip uninstall -y protobuf
pip install protobuf==3.20.3
```

验证：

```bash
python -c "import tensorflow as tf; import google.protobuf; print(tf.__version__, google.protobuf.__version__)"
```

### DLL load failed

**问题：导入 TensorFlow 时出现 `DLL load failed`。**

**处理：**

- 确认安装的是与当前 CUDA、cuDNN 匹配的 `tensorflow-gpu`。
- 本项目默认依赖 `tensorflow-gpu==2.2.0`，通常需要 CUDA 10.1 和 cuDNN 7.x。
- 确认 CUDA、cuDNN 相关 DLL 已加入系统 `PATH`。
- 修改系统环境变量后建议重启终端或电脑。

### h5py decode 报错

**问题：加载 `.h5` 权重时出现与 `decode("utf-8")` 相关的错误。**

**处理：**  
TensorFlow 2.2.0 更适合配合旧版 `h5py` 使用，本项目已锁定：

```text
h5py==2.10.0
```

### Pillow 数组参数报错

**问题：出现 `TypeError: __array__() takes 1 positional argument but 2 were given`。**

**处理：**  
使用项目依赖中的 Pillow 版本：

```text
Pillow==8.2.0
```

## 2. 预测问题

### 图片打不开

**问题：运行 `predict.py` 后输入 `street.jpg`，提示打开失败。**

**处理：**  
脚本从项目根目录按相对路径读取图片。示例图片在 `img` 文件夹下，应输入：

```text
img/street.jpg
```

### shape 不匹配

**问题：加载权重时提示 shape 不匹配。**

**处理：**

- 检查 `yolo.py` 中的 `model_path` 是否指向正确权重。
- 检查 `classes_path` 是否与训练时的类别文件一致。
- 检查 `backbone`、`alpha` 是否与当前权重匹配。
- 如果是自己训练的数据集，训练和预测阶段的类别数量必须一致。

### 没有检测框

**问题：预测可以运行，但没有检测结果。**

**处理：**

- 降低 `yolo.py` 中的 `confidence` 观察是否有框。
- 检查权重文件是否正确加载。
- 检查类别文件是否与权重匹配。
- 检查训练数据标注是否正确。
- 如果是自定义数据集，建议先用训练集中的图片测试。

### 预测图片不显示

**问题：命令行输出了检测结果，但图片窗口没有显示。**

**处理：**

- 确认系统有默认图片查看器。
- 可以在 `predict.py` 中将 `r_image.show()` 改为保存图片，例如 `r_image.save("result.png")`。

## 3. 训练问题

### 显存不足

**问题：训练时报 OOM 或 CUDA out of memory。**

**处理：**

- 减小 `batch_size`。
- 使用更小的输入尺寸。
- 使用更轻量的 backbone。
- 关闭其他占用显存的程序。

### 训练效果不好

**问题：loss 不下降或预测效果很差。**

**处理：**

- 检查 VOC 标注文件是否正确。
- 检查 `2007_train.txt` 中图片路径和标注框是否正确。
- 确认类别文件与训练配置一致。
- 数据量过少时增加数据或使用预训练权重。
- 如果数据分布差异较大，可以适当增加解冻训练轮数。

### 从断点继续训练

**问题：已经训练过一部分，想继续训练。**

**处理：**  
将 `train.py` 中的 `model_path` 指向已有 `.h5` 权重，再继续训练。

## 4. 评估问题

### mAP 为 0

**处理：**

- 先运行 `predict.py`，确认模型能正常检测目标。
- 检查 `get_map.py` 中的类别文件是否与训练一致。
- 检查测试集路径和标注是否正确。
- 检查预测框类别名是否与标注类别名完全一致。

### 路径错误

**问题：出现 `No such file or directory`。**

**处理：**

- 确认从项目根目录运行脚本。
- 检查图片、标注、权重文件路径是否存在。
- 尽量避免路径中出现空格和特殊字符。
- Windows 下如需使用绝对路径，建议使用原始字符串或双反斜杠。

## 5. 其他建议

- 首次运行先使用 `img/street.jpg` 做预测验证。
- 训练、预测、评估使用的类别文件应保持一致。
- 修改网络主干后，权重文件也必须匹配。
- 大文件权重已放在 `model_data/` 中，推送或克隆仓库时需要等待较长时间。
